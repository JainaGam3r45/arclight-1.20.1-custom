logo = [
  ""
  ""
  "   §1    ___ §9     §3     §6___§e  §6   __   /\\"
  "   §1   /   |§9 ____§3____§6/ §e(_)§6__ / /  / /"
  "   §1  / /| |§9/ __§3/ __§6/ §e/ /§6 _ / _ \\/__/"
  "   §1 / ___ §9/ / §3/ /_§6/ §e/ /§6 / / // / /"
  "   §1/_/  |§9/_/  §3\\__§6/_§e/_/§6\\_ /_//_/ /"
  "   §1      §9     §3    §6  §e §6/__/     \\/"
  ""
  "    §aVersion {} / {}"
  "    §aBuild Date {}"
  ""
]
release-name {
  Horn = "Horn"
  GreatHorn = "Great Horn"
  Executions = "Executions"
  Trials = "Trials"
}
java {
  deprecated = [
    "You are running an outdated Java version"
    "Current {0} Recommended {1}"
    "Current Java will not be supported in future"
  ]
}

implementer {
  not-found = "Class not found {}"
  error = "Error occurred {}"
}
i18n {
  current-not-available = "Current locale {0} is not available"
  using-language = "Using locale {0} and fallback locale {1}"
}
loading-mapping = "Loading mappings ..."
mixin-load {
  core = "Arclight core mixin added."
  optimization = "Arclight optimization mixin added."
  skip-conflict = "Skipping {} because mod '{}' is present (mixin conflict avoided)."
  betterchunkloading-move = "BetterChunkLoading PlayerMovementNoUnloaded cannot apply: Arclight overwrites handleMovePlayer for Bukkit PlayerMoveEvent. Other BCL features remain active."
}
mod-load = "Arclight Mod loaded."
patcher {
  loading = "Loading plugin patchers ..."
  loaded = "Loaded {} patchers"
  load-error = "Error occurred while loading patcher"
}
registry {
  forge-event = "Arclight events registered."
  begin = "Registering for Bukkit ..."
  error = "Error occured registering Forge "
  enchantment = "Registered {} enchantments"
  potion = "Registered {} new potion effect types"
  material = "Registered {} new materials with {} blocks and {} items"
  entity-type = "Registered {} new entity types"
  environment = "Registered {} new dimensions"
  villager-profession = "Registered {} new villager professions"
  biome = "Registered {} new biomes"
  summary = "Registered Bukkit registries: {} materials ({} blocks, {} items), {} potions, {} enchantments, {} entities, {} professions, {} biomes, {} dimensions"
  meta-type {
    not-subclass = "{} is not a subclass of {}"
    error = "{} provided itemMetaType {} is invalid: {}"
    no-candidate = "{} did not find a valid constructor in prodived itemMetaType {}"
  }
  block-state {
    not-subclass = "{} is not a subclass of {}"
    error = "{} provided itemMetaType {} is invalid: {}"
    no-candidate = "{} did not find a valid constructor in provided blockStateClass {}"
  }
  entity {
    not-subclass = "{} is not a subclass of {}"
    error = "{} provided entityClass {} is invalid: {}"
  }
}
dfu-disable {
  legacy-plugin = "Legacy plugin is not allowed when optimization.disable-data-fixer is enabled"
  map-convert = "World upgrading is not allowed when optimization.disable-data-fixer is enabled"
}
error-symlink = "File system do not support symbol links"

comments {
  _v.comment = [
    "Repository: https://github.com/IzzelAliz/Arclight"
    "Issue Tracker: https://github.com/IzzelAliz/Arclight/issues"
    ""
    ""
    "Config version number, do not edit."
  ]
  locale.comment = "Language/I18n settings"
  optimization {
    comment = [
      "Optimization related settings (Paper-lite / Spigot-era)"
      "On hybrid stacks, Starlight / ModernFix / Krypton already cover most modern Paper lighting,"
      "general and network optimizations — prefer those mods over reimplementing them in Arclight."
      "Also see spigot.yml (activation/tracking ranges, hopper ticks, merge radius, nerf-spawner-mobs)"
      "and bukkit.yml (spawn-limits, ticks-per.* spawns)."
    ]
    cache-plugin-class.comment = "Cache remapped plugin classes to speed up plugin class loading"
    disable-data-fixer.comment = [
      "Disable DataFixerUpper used for world/data upgrades"
      "Can speed up startup and world load and save roughly 80-200MB of memory"
      "Arclight and its developers are not responsible for any data loss or corruption"
      "Do NOT use in production"
    ]
    goal-selector-update-interval.comment = [
      "Interval in ticks to update the goal selector"
      "Higher values costs less resources"
      "Cause mobs changing their goals less often"
    ]
    use-activation-and-tracking-range.comment = "Whether to use Spigot Activation Range and Tracking Range optimization"
    max-entity-collisions.comment = [
      "Max entity-entity push collisions resolved per living entity per tick (Spigot/Paper-style)"
      "Default 8 matches classic Spigot; <= 0 disables entity push collisions"
      "Respects Bukkit LivingEntity#setCollidable and collidable exemptions"
    ]
    quiet-startup.comment = [
      "Collapse per-registry startup INFO lines into a single summary"
      "Detailed registry counts remain available at DEBUG"
    ]
    spawn {
      comment = [
        "Natural mob generation budget (main thread). Protects TPS under load."
        "Limits spawnForChunk attempts per tick; does not replace bukkit.yml spawn-limits."
        "Ops checklist if caves feel empty: difficulty not Peaceful, gamerule doMobSpawning true,"
        "spigot.yml mob-spawn-range >= 6 (never 0 with use-mob-spawn-range), A/B test enabled=false."
        "Spawner blocks are not budgeted. Does not Folia-split worlds."
      ]
      enabled.comment = "Enable EntityGenerationManager spawn budgeting"
      skip-when-mspt-above.comment = "Skip all natural spawnForChunk work this tick when average MSPT is at or above this value"
      max-chunks-per-tick.comment = "Max chunks that may run NaturalSpawner.spawnForChunk per world per tick (default 64)"
      max-ms-per-tick.comment = "Soft time budget in ms for spawnForChunk work only (excludes tickChunks prep)"
      per-player-mob-spawns.comment = [
        "Paper-lite: LocalMobCapCalculator for local density, plus bukkit.yml spawn-limits as a world ceiling"
        "Reduces one farm eating the whole world mob cap while still honoring high admin limits"
      ]
      use-mob-spawn-range.comment = "Honor spigot.yml mob-spawn-range when choosing chunks for natural spawn (0 = no natural spawns)"
      relaxed-when-players-at-most.comment = "When online players are at or below this count and MSPT is low, skip max-chunks/max-ms caps (still honors skip-when-mspt-above and mob-spawn-range)"
      relaxed-when-mspt-below.comment = "MSPT must be below this value to enable relaxed solo/low-pop spawn budgeting"
      local-mob-cap-scale.comment = [
        "Multiplies vanilla LocalMobCapCalculator max per category (monsters ~70 at 1.0)"
        "bukkit.yml spawn-limits stay the world ceiling; this only raises/lowers local density"
        "Example: 3.0 ≈ 210 local monsters. Edit the live arclight.conf (jar defaults do not overwrite)"
      ]
      local-mob-cap.comment = [
        "Absolute local caps per category; -1 means use vanillaMax * local-mob-cap-scale"
        "Example: monster = 200 caps local hostiles at 200 regardless of scale"
      ]
    }
    chunks {
      comment = [
        "Chunk load / generate / send budget on the main thread. Protects TPS while exploring or teleporting."
        "Soft-defers non-blocking scheduleChunkGeneration under pressure; sync getChunk always flushes deferred work first."
        "NEVER gates MainThreadExecutor.pollTask (that caused ServerWatchdog deadlocks)."
        "Also tune spigot.yml view-distance (client packets) and simulation-distance (ticking chunks)."
        "Avoid keep-spawn-loaded on empty Multiverse worlds. Use /chunkcap to inspect queues."
      ]
      enabled.comment = "Enable ChunkWorkloadManager budgeting"
      skip-gen-when-mspt-above.comment = "Defer new worldgen schedules this tick when average MSPT is at or above this value (sync getChunk still forces progress)"
      max-chunk-loads-per-tick.comment = "Max non-blocking light/load-style schedule promotions per server tick (sync path ignores the cap)"
      max-chunk-gens-per-tick.comment = "Max non-blocking worldgen schedule promotions per server tick (sync path ignores the cap)"
      max-chunk-sends-per-tick.comment = "Max chunk+light packets sent to players per server tick (smooths TP/join floods)"
      max-ms-per-tick.comment = "Soft time budget in milliseconds for budgeted chunk work per server tick"
    }
  }
  async-catcher.comment = [
    "Async Catcher related settings"
    "There are four modes, and BLOCK is recommended"
    "NONE - Do nothing"
    "DISPATCH - Non blocking-ly dispatch action to main thread"
    "BLOCK - Run in main thread and wait for results"
    "EXCEPTION - Raise an error"
  ]
  async-catcher.dump.comment = "Dump stack trace information in debug.log"
  compatibility {
    symlink-world.comment = [
      "Create symbol links to mod dimension folder that matches Bukkit world name"
      "Enable this could improve plugin compatibility"
      "Changing this on production server will cause changes to mod world names"
      "  and cause data loss on plugins relying world names"
      "See https://github.com/IzzelAliz/Arclight/wiki/World-Symlink for more detail"
    ]
    extra-logic-worlds.comment = [
      "Extra worlds running logic"
      "If any mods do not function well, try search class names in logs related to [EXT_LOGIC] and add them here"
    ]
    forward-permission.comment = [
      "true - Forward Forge permission query to Bukkit"
      "false - Disable permission forward"
      "reverse - Forward Bukkit player permission query to Forge"
    ]
    valid-username-regex.comment = [
      "Regex for valid username check. Leave it blank will fallback to vanilla check"
      "Following allows Chinese characters:"
      "valid-username-regex = \"^[ -~\\\\p{sc=Han}]{1,16}$\""
      "Following allows any username to login:"
      "valid-username-regex = \".+\""
    ]
    lenient-item-tag-match.comment = [
      "Allows items with an empty nbt tag stack on no tag items"
    ]
    isolate-plugin-class-loaders.comment = [
      "An illegal access means that a plugin loads a class from another plugin that is not a depend or soft-depend of itself"
      "When a package name is given, no illegal access will be attempted on classes prefixed with the package name."
      "This is especially useful when class loading issues happen."
      "Beware that it may break plugins relying on illegal access."
    ]
    isolate-adventure-from-modloader.comment = [
      "Prevent plugins from loading Kyori Adventure from ModLoader."
      "This is only an compatibility option and persists for now. Will be replaced with a better mechanism."
    ]
  }
}
